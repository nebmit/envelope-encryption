import {
  changeKeyphrase,
  decryptData,
  encryptData,
  generateKey,
  initialize,
} from './default';
import simple from './simple';

export {
  initialize,
  changeKeyphrase,
  generateKey,
  encryptData,
  decryptData,
  simple,
};
